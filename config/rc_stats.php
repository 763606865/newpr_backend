<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Redis Connection
    |--------------------------------------------------------------------------
    |
    | 浏览量统计使用的 Redis 连接名，对应 config/database.php 中 redis 配置。
    |
    */

    'redis_connection' => env('RC_STATS_REDIS_CONNECTION', 'default'),

    /*
    |--------------------------------------------------------------------------
    | Key Prefix
    |--------------------------------------------------------------------------
    |
    | Redis Key 前缀。完整 Key 形如：
    | {prefix}:job:{id}:pv:{Y-m-d}
    | {prefix}:job:{id}:uv:{Y-m-d}
    | {prefix}:resume:{id}:pv:{Y-m-d}
    | {prefix}:resume:{id}:uv:{Y-m-d}
    |
    */

    'key_prefix' => env('RC_STATS_KEY_PREFIX', 'rc:view'),

    /*
    |--------------------------------------------------------------------------
    | Key TTL (days)
    |--------------------------------------------------------------------------
    |
    | 日统计 Key 自统计日起保留天数，供归档任务读取后过期。
    |
    */

    'key_ttl_days' => (int) env('RC_STATS_KEY_TTL_DAYS', 8),

    /*
    |--------------------------------------------------------------------------
    | Sync Batch Size
    |--------------------------------------------------------------------------
    |
    | 从 Redis 同步至 MySQL 时，每个队列 Job 处理的实体数量。
    |
    */

    'sync_batch_size' => (int) env('RC_STATS_SYNC_BATCH_SIZE', 100),

];
