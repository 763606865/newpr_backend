# 数据库表结构

> 用途：中台搭建时对接各分站主数据结构（首批：`companies`、`departments`、`employees`、`users`、`rc_resumes`、`cms_announcements`）。  
> 来源：`database/migrations/2026_04_03_022020_create_company_tables.php`、`database/migrations/2026_06_04_164129_create_company_info_tables.php`、`database/migrations/0001_01_01_000000_create_users_table.php`、`database/migrations/2026_05_28_090438_create_rc_tables.php`、`database/migrations/2026_05_27_172810_create_cms_tables.php` 以及对应 Enum。

## 统一约定

- 时间字段统一为 `timestamp`（或 `datetime`）类型。
- 能用 `int` 的枚举字段尽量用 `int`。
- 软删除字段统一为 `deleted_at`。
- 以下结构为对接视角（字段名与库表保持一致）。

---

## 1) companies（企业主体表）

```jsonc
{
  "id": 1, // bigint unsigned
  "parent_id": null, // bigint unsigned|null, 上级企业ID（集团总部/独立企业为 null）
  "depth": 1, // int, 集团层级深度（1=集团根或独立企业）
  "name": "企业名称", // string
  "credit_code": "统一社会信用代码", // string|null, unique
  "legal_person": "法人姓名", // string|null
  "contact_phone": "联系电话", // string|null
  "address": "企业地址", // text|null
  "status": 1, // int
  "statusList": [
    { "id": 0, "name": "禁用" },
    { "id": 1, "name": "启用" },
    { "id": 2, "name": "审批中" }
  ],
  "created_at": "创建时间", // timestamp
  "updated_at": "更新时间", // timestamp
  "deleted_at": "删除时间" // timestamp|null
}
```

### 约束/索引

- 唯一索引：`credit_code`。
- 普通索引：`parent_id`、`(parent_id, status)`。
- `parent_id = null` 表示集团总部或独立法人；子公司 `parent_id` 指向集团企业 `id`。

### DDL（参考）

```sql
create table newpr.companies
(
    id            bigint unsigned auto_increment
        primary key,
    parent_id     bigint unsigned   null comment '上级企业ID（集团总部/独立企业为 null，子公司指向集团）',
    depth         tinyint unsigned  default 1 not null comment '集团层级深度（1=集团根或独立企业）',
    name          varchar(255)      not null comment '企业名称',
    credit_code   varchar(255)      null comment '统一社会信用代码',
    legal_person  varchar(255)      null comment '法人姓名',
    contact_phone varchar(255)      null comment '联系电话',
    address       text              null comment '企业地址',
    status        tinyint default 1 not null comment '状态',
    created_at    timestamp         null,
    updated_at    timestamp         null,
    deleted_at    timestamp         null,
    constraint companies_credit_code_unique
        unique (credit_code),
    index companies_parent_id_index (parent_id),
    index companies_parent_id_status_index (parent_id, status)
)
    comment '企业主体表' collate = utf8mb4_unicode_ci;
```

---

## 1.1) company_licenses（企业证件表）

```jsonc
{
  "id": 1,
  "company_id": 1,
  "license_type": 1, // 1-营业执照, 2-食品经营许可证, 3-资质证书, 4-其他
  "name": "营业执照",
  "license_no": "证件编号",
  "issuer": "发证机关",
  "issue_date": "2020-01-01",
  "expire_date": null,
  "file_url": "uploads/...",
  "file_name": "license.pdf",
  "file_ext": "pdf",
  "is_primary": 1,
  "sort": 0,
  "status": 1,
  "remark": null,
  "extra": null
}
```

- 索引：`company_id`、`license_type`、`(company_id, license_type)`、`(company_id, status)`、`issue_date`、`expire_date`。

---

## 1.2) company_contacts（企业联系人/股东表）

```jsonc
{
  "id": 1,
  "company_id": 1,
  "contact_type": 2, // 1-法定代表人, 2-股东, 3-联系人, 4-实际控制人, 5-其他
  "name": "张三",
  "id_card": "360101199001011234",
  "phone": "13800000000",
  "email": "zhang@example.com",
  "position": "执行董事",
  "share_ratio": 60.5,
  "address": "联系地址",
  "is_primary": 1,
  "sort": 0,
  "status": 1,
  "remark": null,
  "extra": null
}
```

- 索引：`company_id`、`contact_type`、`(company_id, contact_type)`、`(company_id, status)`、`id_card`。
- `share_ratio` 仅股东等类型使用，单位为百分比。

---

## 2) departments（企业部门架构表）

```jsonc
{
  "id": 1, // bigint unsigned
  "company_id": 1, // bigint unsigned
  "parent_id": 0, // bigint unsigned, 上级部门ID（0表示根部门）
  "depth": 1, // int, 层级
  "name": "部门名称", // string
  "type": 1, // int
  "typeList": [
    { "id": 1, "name": "职能" },
    { "id": 2, "name": "业务" },
    { "id": 3, "name": "管理层" }
  ],
  "sort": 0, // int
  "remark": "备注", // string|null
  "created_at": "创建时间", // timestamp
  "updated_at": "更新时间", // timestamp
  "deleted_at": "删除时间" // timestamp|null
}
```

### 约束/索引

- 普通索引：`(company_id, parent_id)`。

### DDL（参考）

```sql
create table newpr.departments
(
    id         bigint unsigned auto_increment
        primary key,
    company_id bigint unsigned                not null,
    parent_id  bigint unsigned default 0      not null comment '上级部门ID',
    depth      tinyint          default 1     null comment '层级',
    name       varchar(255)                  not null comment '部门名称',
    type       tinyint          default 1     not null comment '类型',
    sort       int              default 0     not null,
    remark     varchar(255)                  null,
    created_at timestamp                     null,
    updated_at timestamp                     null,
    deleted_at timestamp                     null,
    index departments_company_id_parent_id_index (company_id, parent_id)
)
    comment '企业部门架构表' collate = utf8mb4_unicode_ci;
```

---

## 3) employees（员工表）

```jsonc
{
  "id": 1, // bigint unsigned
  "user_id": 1, // bigint unsigned|null, 关联users.id
  "company_id": 1, // bigint unsigned|null, 关联companies.id
  "department_id": 1, // bigint unsigned|null, 关联departments.id
  "position_id": 1, // bigint unsigned|null
  "employee_no": "员工工号", // string|null
  "real_name": "姓名", // string
  "avatar": "头像地址", // string
  "email": "邮箱", // string|null
  "mobile": "手机号", // string|null
  "status": 1, // int
  "statusList": [
    { "id": 0, "name": "离职" },
    { "id": 1, "name": "在职" }
  ],
  "entry_time": "入职时间", // timestamp|null
  "created_at": "创建时间", // timestamp
  "updated_at": "更新时间", // timestamp
  "deleted_at": "删除时间" // timestamp|null
}
```

### 约束/索引

- 普通索引：`company_id`、`department_id`、`position_id`、`user_id`、`entry_time`。

### DDL（参考）

```sql
create table newpr.employees
(
    id            bigint unsigned auto_increment
        primary key,
    user_id       bigint unsigned                null comment '关联用户ID',
    company_id    bigint unsigned                null comment '企业ID',
    department_id bigint unsigned                null comment '部门ID',
    position_id   bigint unsigned                null comment '职位ID',
    employee_no   varchar(255)                   null comment '员工工号',
    real_name     varchar(255)                   not null comment '姓名',
    avatar        varchar(255)                   not null comment '头像',
    email         varchar(255)                   null comment '邮箱',
    mobile        varchar(255)                   null comment '手机号',
    status        tinyint          default 1     not null comment '状态',
    entry_time    timestamp                      null comment '加入时间',
    created_at    timestamp                      null,
    updated_at    timestamp                      null,
    deleted_at    timestamp                      null,
    index employees_company_id_index (company_id),
    index employees_department_id_index (department_id),
    index employees_position_id_index (position_id),
    index employees_user_id_index (user_id),
    index employees_entry_time_index (entry_time)
)
    comment '员工表' collate = utf8mb4_unicode_ci;
```

---

## 4) users（用户表）

```jsonc
{
  "id": 1, // bigint unsigned
  "uuid": "二进制16位UUID", // binary(16), unique
  "name": "真实姓名", // string(50)|null
  "nickname": "昵称", // string(50)|null
  "phone": "手机号", // string(30), unique
  "email": "邮箱", // string(100)
  "avatar": "头像地址", // string
  "gender": 1, // int
  "genderList": [
    { "id": 0, "name": "未知" },
    { "id": 1, "name": "男" },
    { "id": 2, "name": "女" }
  ],
  "password": "密码哈希", // string|null
  "status": "active", // string
  "statusList": [
    { "id": "active", "name": "激活" },
    { "id": "resolved", "name": "修复中" },
    { "id": "closed", "name": "已关闭" }
  ],
  "wechat_unionid": "微信UnionID", // string(64)|null, unique
  "extra": "扩展信息", // mediumText|null
  "last_login_ip": "最后登录IP", // string|null
  "last_login_at": "最后登录时间", // timestamp|null
  "created_at": "创建时间", // timestamp
  "updated_at": "更新时间", // timestamp
  "deleted_at": "删除时间" // timestamp|null
}
```

### 约束/索引

- 唯一索引：`uuid`、`phone`、`wechat_unionid`。

### DDL（参考）

```sql
create table newpr.users
(
    id            bigint unsigned auto_increment
        primary key,
    uuid          binary(16)                       not null comment '全局唯一用户标识(UUID v4/v7)',
    name          varchar(50)                      null comment '真实姓名',
    nickname      varchar(50)                      null comment '昵称',
    phone         varchar(30)                      not null,
    email         varchar(100)                     not null,
    avatar        varchar(255)                     not null comment '头像',
    gender        tinyint            default 0     null comment '性别',
    password      varchar(255)                     null comment '密码',
    status        varchar(255)       default 'active' not null comment '用户状态',
    wechat_unionid varchar(64)                     null comment '微信UnionID',
    extra         mediumtext                       null comment '扩展信息',
    last_login_ip varchar(255)                     null comment '最后登录IP',
    last_login_at timestamp                        null comment '最后登录时间',
    created_at    timestamp                        null,
    updated_at    timestamp                        null,
    deleted_at    timestamp                        null,
    constraint users_uuid_unique
        unique (uuid),
    constraint users_phone_unique
        unique (phone),
    constraint users_wechat_unionid_unique
        unique (wechat_unionid)
)
    collate = utf8mb4_unicode_ci;
```

---

## 5) rc_resumes（简历主表）

```jsonc
{
  "id": 1, // bigint unsigned
  "user_id": 1, // bigint unsigned, 关联users.id
  "resume_no": "RC202606040001", // string(64), 与user_id联合唯一
  "title": "高级后端工程师-主简历", // string
  "full_name": "张三", // string(50)
  "gender": 1, // int
  "genderList": [
    { "id": 0, "name": "未知" },
    { "id": 1, "name": "男" },
    { "id": 2, "name": "女" }
  ],
  "id_card": "110101199001011234", // char(18)|null
  "nation": "汉族", // string(20)
  "avatar": "https://example.com/avatar.jpg", // string|null
  "birth_date": "1990-01-01", // date|null
  "birth_month": "1990-01", // char(7)|null
  "age": 36, // int|null
  "marital_status": 1, // int
  "maritalStatusList": [
    { "id": 0, "name": "未知" },
    { "id": 1, "name": "未婚" },
    { "id": 2, "name": "已婚" },
    { "id": 3, "name": "离异" },
    { "id": 4, "name": "丧偶" }
  ],
  "political_status": "群众", // string(20)
  "native_place": "广东省深圳市南山区", // string(100)|null
  "current_identity": 1, // int
  "work_start_date": "2014-07-01", // date|null
  "work_years": 12, // int|null
  "current_salary": "30K", // string(50)|null
  "salary_remark": "税前*14薪", // string(200)|null
  "recruit_source": "Boss直聘", // string(100)|null
  "highest_education_level": 3, // int|null
  "educationLevelList": [
    { "id": 1, "name": "高中/中专" },
    { "id": 2, "name": "专科" },
    { "id": 3, "name": "本科" },
    { "id": 4, "name": "硕士" },
    { "id": 5, "name": "博士" },
    { "id": 6, "name": "其他" }
  ],
  "is_fresh_graduate": 0, // int, 1-是, 0-否
  "expected_salary_min": 25000.00, // decimal(10,2)|null
  "expected_salary_max": 35000.00, // decimal(10,2)|null
  "expected_salary_unit": 1, // int
  "expectedSalaryUnitList": [
    { "id": 1, "name": "月" },
    { "id": 2, "name": "日" },
    { "id": 3, "name": "时" }
  ],
  "household_register": "广东省-深圳市", // string(100)|null
  "household_register_detail": "广东省-深圳市-南山区", // string(200)|null
  "current_residence_city": "广东省-深圳市-南山区", // string(100)|null
  "current_city_code": "440305", // string(32)|null
  "current_residence_detail": "科技园某路1号", // string(200)|null
  "residence_country": "中国", // string(50)
  "phone": "13800001111", // string(20)
  "email": "demo@example.com", // string(100)
  "source_type": 1, // int
  "sourceTypeList": [
    { "id": 1, "name": "上传" },
    { "id": 2, "name": "解析" },
    { "id": 3, "name": "手工创建" },
    { "id": 4, "name": "导入" }
  ],
  "file_url": "https://example.com/resume.pdf", // string|null
  "file_name": "resume.pdf", // string|null
  "file_ext": "pdf", // string(16)|null
  "text_content": "简历文本...", // longText|null
  "parsed_data": { "skills": ["PHP", "Laravel"] }, // json|null
  "is_primary": 1, // int, 1-是, 0-否
  "status": 1, // int
  "statusList": [
    { "id": 1, "name": "正常" },
    { "id": 0, "name": "停用" }
  ],
  "extra": { "source": "portal" }, // json|null
  "created_at": "创建时间", // timestamp
  "updated_at": "更新时间", // timestamp
  "deleted_at": "删除时间" // timestamp|null
}
```

### 约束/索引

- 唯一索引：`(user_id, resume_no)`。
- 普通索引：`user_id`、`full_name`、`phone`、`email`、`id_card`、`age`、`work_years`、`highest_education_level`、`is_fresh_graduate`、`current_city_code`、`(user_id, is_primary)`、`(user_id, status)`、`(expected_salary_min, expected_salary_max)`。

### DDL（参考）

```sql
create table newpr.rc_resumes
(
    id                      bigint unsigned auto_increment
        primary key,
    user_id                 bigint unsigned                  not null comment '关联用户ID',
    resume_no               varchar(64)                      not null comment '简历编号',
    title                   varchar(255)                     not null comment '简历名称',
    full_name               varchar(50)                      not null comment '姓名',
    gender                  tinyint            default 0     not null comment '性别',
    id_card                 char(18)                         null comment '身份证号',
    nation                  varchar(20)        default '汉族' not null comment '民族',
    avatar                  varchar(255)                     null comment '头像',
    birth_date              date                             null comment '完整出生日期（格式：YYYY-MM-DD）',
    birth_month             char(7)                          null comment '出生年月（格式：YYYY-MM）',
    age                     tinyint unsigned                 null comment '年龄',
    marital_status          tinyint            default 0     not null comment '婚姻状况:0-未知，1-未婚，2-已婚，3-离异，4-丧偶',
    political_status        varchar(20)        default '群众' not null comment '政治面貌',
    native_place            varchar(100)                     null comment '籍贯（完整省市区）',
    current_identity        tinyint            default 0     not null comment '当前身份:职场人, 学生，待业，0-其他',
    work_start_date         date                             null comment '参加工作日期（用于计算工作年限）',
    work_years              tinyint unsigned                 null comment '工作年限（年）',
    current_salary          varchar(50)                      null comment '当前/期望薪资',
    salary_remark           varchar(200)                     null comment '薪资备注（如20K·14薪、税前/税后等）',
    recruit_source          varchar(100)                     null comment '招聘信息获取来源',
    highest_education_level tinyint                          null comment '最高学历: 1-高中/中专, 2-专科, 3-本科, 4-硕士, 5-博士, 6-其他',
    is_fresh_graduate       tinyint            default 0     not null comment '是否应届生: 1-是, 0-否',
    expected_salary_min     decimal(10, 2)                   null comment '期望薪资下限',
    expected_salary_max     decimal(10, 2)                   null comment '期望薪资上限',
    expected_salary_unit    tinyint            default 1     not null comment '期望薪资单位: 1-月, 2-日, 3-时',
    household_register      varchar(100)                     null comment '户口所在地（精简版，如省-市）',
    household_register_detail varchar(200)                   null comment '户口所在地详细地址（省-市-区县）',
    current_residence_city  varchar(100)                     null comment '现居住城市（省-市-区县）',
    current_city_code       varchar(32)                      null comment '现居住城市编码',
    current_residence_detail varchar(200)                    null comment '现居住地详细地址',
    residence_country       varchar(50)        default '中国' not null comment '现居住国家/地区',
    phone                   varchar(20)                      not null comment '联系电话',
    email                   varchar(100)                     not null comment '电子邮箱',
    source_type             tinyint            default 1     not null comment '来源类型: 1-上传, 2-解析, 3-手工创建, 4-导入',
    file_url                varchar(255)                     null comment '简历文件地址',
    file_name               varchar(255)                     null comment '简历文件名称',
    file_ext                varchar(16)                      null comment '文件后缀',
    text_content            longtext                         null comment '简历文本内容',
    parsed_data             json                             null comment '解析后的结构化数据',
    is_primary              tinyint            default 0     not null comment '是否主简历: 1-是, 0-否',
    status                  tinyint            default 1     not null comment '状态: 1-正常, 0-停用',
    extra                   json                             null comment '扩展字段',
    created_at              timestamp                        null,
    updated_at              timestamp                        null,
    deleted_at              timestamp                        null,
    constraint rc_resumes_user_id_resume_no_unique
        unique (user_id, resume_no),
    index rc_resumes_user_id_index (user_id),
    index rc_resumes_full_name_index (full_name),
    index rc_resumes_phone_index (phone),
    index rc_resumes_email_index (email),
    index rc_resumes_id_card_index (id_card),
    index rc_resumes_user_id_is_primary_index (user_id, is_primary),
    index rc_resumes_user_id_status_index (user_id, status),
    index rc_resumes_age_index (age),
    index rc_resumes_work_years_index (work_years),
    index rc_resumes_highest_education_level_index (highest_education_level),
    index rc_resumes_is_fresh_graduate_index (is_fresh_graduate),
    index rc_resumes_expected_salary_min_expected_salary_max_index (expected_salary_min, expected_salary_max),
    index rc_resumes_current_city_code_index (current_city_code)
)
    comment '招聘简历表' collate = utf8mb4_unicode_ci;
```

---

## 6) cms_announcements（公告表）

```jsonc
{
  "id": 1, // bigint unsigned
  "city_code": "440300", // string(32)|null, 为空表示全站可用
  "title": "关于五一期间服务安排的公告", // string
  "sub_title": "2026年第2号", // string|null
  "link_url": "https://example.com/notice/1", // string|null
  "type": 1, // int
  "typeList": [
    { "id": 1, "name": "自发公告" },
    { "id": 2, "name": "站点采集" },
    { "id": 3, "name": "授权公告" }
  ],
  "source_name": "XX人社局", // string|null
  "source_url": "https://xxx.gov.cn", // string|null
  "summary": "公告摘要", // string(1000)|null
  "content": "公告正文", // longText|null
  "published_at": "发布时间", // timestamp|null
  "start_at": "生效开始时间", // timestamp|null
  "end_at": "生效结束时间", // timestamp|null
  "is_top": 0, // int, 1-是, 0-否
  "status": 2, // int
  "statusList": [
    { "id": 1, "name": "草稿" },
    { "id": 2, "name": "已发布" },
    { "id": 3, "name": "下线" }
  ],
  "sort": 0, // int
  "extra": { "source": "cms" }, // json|null
  "created_at": "创建时间", // timestamp
  "updated_at": "更新时间", // timestamp
  "deleted_at": "删除时间" // timestamp|null
}
```

### 约束/索引

- 普通索引：`city_code`、`published_at`。

### DDL（参考）

```sql
create table newpr.cms_announcements
(
    id           bigint unsigned auto_increment
        primary key,
    city_code    varchar(32)                     null comment '城市编码(为空表示全站可用)',
    title        varchar(255)                    not null comment '公告标题',
    sub_title    varchar(255)                    null comment '公告副标题',
    link_url     varchar(255)                    null comment '公告链接',
    type         tinyint           default 1     not null comment '公告类型: 1-自发公告, 2-站点采集, 3-授权公告',
    source_name  varchar(255)                    null comment '来源站点/机构名称',
    source_url   varchar(255)                    null comment '来源地址',
    summary      varchar(1000)                   null comment '公告摘要',
    content      longtext                        null comment '公告正文',
    published_at timestamp                       null comment '发布时间',
    start_at     timestamp                       null comment '生效开始时间',
    end_at       timestamp                       null comment '生效结束时间',
    is_top       tinyint           default 0     not null comment '是否置顶: 1-是, 0-否',
    status       tinyint           default 1     not null comment '状态: 1-草稿, 2-已发布, 3-下线',
    sort         int               default 0     not null comment '排序',
    extra        json                             null comment '扩展字段',
    created_at   timestamp                        null,
    updated_at   timestamp                        null,
    deleted_at   timestamp                        null,
    index cms_announcements_city_code_index (city_code),
    index cms_announcements_published_at_index (published_at)
)
    comment '门户公告表' collate = utf8mb4_unicode_ci;
```

---

## 7) 关系参考（中台对接建议）

- `companies.id` 1 - n `departments.company_id`
- `companies.id` 1 - n `employees.company_id`
- `departments.id` 1 - n `employees.department_id`
- `users.id` 1 - n `employees.user_id`
- `users.id` 1 - n `rc_resumes.user_id`

> 说明：当前迁移中未显式声明外键约束，但业务上按上述关系关联。
