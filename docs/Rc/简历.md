# 简历

## 说明

- 路由前缀：`/rc`
- 鉴权：均需 `Bearer Token`，并使用 `auth:rc`
- 适用对象：求职者身份
- 成功响应：`api_response()`，结构为 `code` + `data` + `meta`
- 业务错误：`error()`，结构为 `code` + `message` + `meta`

---

## 公共数据结构

### 简历对象（`RcResumeResource`）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | 简历 ID |
| `user_id` | int | 所属用户 ID |
| `resume_no` | string | 简历编号 |
| `title` | string | 简历名称 |
| `full_name` | string | 姓名 |
| `avatar` | string\|null | 头像 OSS 路径 |
| `gender` | int | 性别 |
| `id_card` | string\|null | 身份证号 |
| `nation` | string | 民族 |
| `birth_date` | string\|null | 出生日期 |
| `birth_month` | string\|null | 出生年月（`YYYY-MM`） |
| `age` | int\|null | 年龄 |
| `marital_status` | int | 婚姻状况 |
| `political_status` | string | 政治面貌 |
| `native_place` | string\|null | 籍贯 |
| `current_identity` | int | 当前身份 |
| `work_start_date` | string\|null | 参加工作日期 |
| `work_years` | int\|null | 工作年限 |
| `current_salary` | string\|null | 当前/期望薪资 |
| `salary_remark` | string\|null | 薪资备注 |
| `recruit_source` | string\|null | 招聘信息获取来源 |
| `highest_education_level` | int\|null | 最高学历 |
| `is_fresh_graduate` | int | 是否应届生（`0/1`） |
| `expected_salary_min` | number\|null | 期望薪资下限 |
| `expected_salary_max` | number\|null | 期望薪资上限 |
| `expected_salary_unit` | int | 期望薪资单位 |
| `household_register` | string\|null | 户口所在地 |
| `household_register_detail` | string\|null | 户口详细地址 |
| `current_residence_city` | string\|null | 现居住城市 |
| `current_city_code` | string\|null | 现居住城市编码 |
| `current_residence_detail` | string\|null | 现居住地详细地址 |
| `residence_country` | string\|null | 现居住国家/地区 |
| `phone` | string | 联系电话 |
| `email` | string | 电子邮箱 |
| `source_type` | int | 来源类型 |
| `file_url` | string\|null | 简历文件地址 |
| `file_name` | string\|null | 简历文件名称 |
| `file_ext` | string\|null | 文件后缀 |
| `text_content` | string\|null | 简历文本内容 |
| `parsed_data` | object\|null | 结构化解析结果 |
| `is_primary` | int | 是否主简历（`0/1`） |
| `status` | int | 状态 |
| `extra` | object\|null | 扩展字段 |
| `created_at` | string\|null | 创建时间 |
| `updated_at` | string\|null | 更新时间 |

### 工作经历对象（`RcResumeWorkResource`）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | 工作经历 ID |
| `resume_id` | int | 所属简历 ID |
| `user_id` | int | 所属用户 ID |
| `company_name` | string | 公司名称 |
| `department` | string\|null | 部门名称 |
| `position` | string | 职位名称 |
| `employment_type` | int\|null | 工作类型 |
| `start_date` | string\|null | 开始日期 |
| `end_date` | string\|null | 结束日期 |
| `is_current` | int | 是否至今（`0/1`） |
| `description` | string\|null | 工作描述 |
| `sort` | int | 排序值 |
| `extra` | object\|null | 扩展字段 |
| `created_at` | string\|null | 创建时间 |
| `updated_at` | string\|null | 更新时间 |

### 教育经历对象（`RcResumeEducationResource`）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | 教育经历 ID |
| `resume_id` | int | 所属简历 ID |
| `user_id` | int | 所属用户 ID |
| `school_name` | string | 学校名称 |
| `major` | string\|null | 专业名称 |
| `degree` | int\|null | 学历 |
| `education_type` | int | 学习方式 |
| `start_date` | string\|null | 开始日期 |
| `end_date` | string\|null | 结束日期 |
| `is_current` | int | 是否至今（`0/1`） |
| `description` | string\|null | 经历描述 |
| `sort` | int | 排序值 |
| `extra` | object\|null | 扩展字段 |
| `created_at` | string\|null | 创建时间 |
| `updated_at` | string\|null | 更新时间 |

### 枚举值

#### `gender`

| 值 | 含义 |
|----|------|
| `0` | 未知 |
| `1` | 男 |
| `2` | 女 |

#### `marital_status`

| 值 | 含义 |
|----|------|
| `0` | 未知 |
| `1` | 未婚 |
| `2` | 已婚 |
| `3` | 离异 |
| `4` | 丧偶 |

#### `current_identity`

| 值 | 含义 |
|----|------|
| `0` | 其他 |
| `1` | 职场人 |
| `2` | 学生 |
| `3` | 待业 |

#### `highest_education_level`

| 值 | 含义 |
|----|------|
| `1` | 高中/中专 |
| `2` | 专科 |
| `3` | 本科 |
| `4` | 硕士 |
| `5` | 博士 |
| `6` | 其他 |

#### `expected_salary_unit`

| 值 | 含义 |
|----|------|
| `1` | 月 |
| `2` | 日 |
| `3` | 时 |

#### `source_type`

| 值 | 含义 |
|----|------|
| `1` | 上传 |
| `2` | 解析 |
| `3` | 手工创建 |
| `4` | 导入 |

#### `status`

| 值 | 含义 |
|----|------|
| `1` | 正常 |
| `0` | 停用 |

#### `employment_type`

| 值 | 含义 |
|----|------|
| `1` | 全职 |
| `2` | 兼职 |
| `3` | 实习 |

#### `education_type`

| 值 | 含义 |
|----|------|
| `1` | 全日制 |
| `2` | 非全日制 |

#### `degree`

- 与 `highest_education_level` 枚举值一致：`1=高中/中专`、`2=专科`、`3=本科`、`4=硕士`、`5=博士`、`6=其他`

### 新增时常用默认值

| 字段 | 默认值 | 说明 |
|------|--------|------|
| `source_type` | `3` | 控制器默认手工创建 |
| `status` | `1` | 正常 |
| `is_primary` | `0` | 但首份简历会被自动置为 `1` |

### 工作经历 / 教育经历默认值

| 对象 | 字段 | 默认值 | 说明 |
|------|------|--------|------|
| 工作经历 | `employment_type` | `1` | 全职 |
| 工作经历 | `is_current` | `0` | 默认非在职中 |
| 工作经历 | `sort` | `0` | 默认排序值 |
| 教育经历 | `education_type` | `1` | 全日制 |
| 教育经历 | `is_current` | `0` | 默认非在读 |
| 教育经历 | `sort` | `0` | 默认排序值 |

---

## 1) 简历列表

- 接口：`GET /rc/resumes`
- 描述：返回当前登录用户的简历列表，按 `is_primary DESC -> updated_at DESC -> id DESC` 排序

### Query 参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `per_page` | int | `15` | 每页条数，最小 `1`，最大 `100` |

### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "current_page": 1,
    "data": [
      {
        "id": 1,
        "user_id": 1,
        "resume_no": "RC20260602120000ABC123",
        "title": "求职简历",
        "full_name": "张三",
        "gender": 1,
        "marital_status": 1,
        "current_identity": 1,
        "highest_education_level": 3,
        "expected_salary_unit": 1,
        "source_type": 3,
        "is_primary": 1,
        "status": 1,
        "created_at": "2026-06-02T12:00:00.000000Z",
        "updated_at": "2026-06-02T12:00:00.000000Z"
      }
    ],
    "first_page_url": "http://example.com/rc/resumes?page=1",
    "from": 1,
    "last_page": 1,
    "last_page_url": "http://example.com/rc/resumes?page=1",
    "links": [],
    "next_page_url": null,
    "path": "http://example.com/rc/resumes",
    "per_page": 15,
    "prev_page_url": null,
    "to": 1,
    "total": 1
  },
  "meta": {
    "timestamp": 1748865600.1234,
    "response_time": 0.0123
  }
}
```

### 规则

- 仅返回当前 `auth:rc` 用户自己的简历
- 列表项通过 `RcResumeResource` 序列化，位于 `data.data`

---

## 2) 简历详情

- 接口：`GET /rc/resumes/{id}`
- 描述：返回当前登录用户指定简历

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | int | 简历 ID |

### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "id": 1,
    "user_id": 1,
    "resume_no": "RC20260602120000ABC123",
    "title": "求职简历",
    "full_name": "张三",
    "source_type": 3,
    "is_primary": 1,
    "status": 1,
    "created_at": "2026-06-02T12:00:00.000000Z",
    "updated_at": "2026-06-02T12:00:00.000000Z"
  },
  "meta": {
    "timestamp": 1748865600.1234,
    "response_time": 0.0123
  }
}
```

### 业务失败响应示例

```json
{
  "code": 404,
  "message": "简历不存在。",
  "meta": {
    "timestamp": 1748865600.1234,
    "response_time": 0.0123
  }
}
```

### 规则

- 仅允许查看当前用户自己的简历
- 找不到记录或越权访问时返回 `404`

---

## 3) 新增简历

- 接口：`POST /rc/resumes`
- 描述：新增一份简历

### 请求示例

```json
{
  "title": "求职简历",
  "full_name": "张三",
  "phone": "13800138000",
  "email": "zhangsan@example.com",
  "gender": 1,
  "is_primary": 0,
  "status": 1,
  "source_type": 3
}
```

### 字段规则

- 以下规则与 `app/Rc/Requests/ResumeStoreRequest.php` 对齐：

| 字段 | 是否必填 | 类型/规则 | 说明 |
|------|----------|-----------|------|
| `full_name` | 是 | `string`, `max:50` | 姓名 |
| `avatar` | 否 | `string`, `max:255` | 头像 OSS 路径；可先调用 `POST /rc/upload` 上传后写入 |
| `gender` | 否 | `integer`, `min:0`, `max:2` | 性别枚举：`0=未知`、`1=男`、`2=女` |
| `id_card` | 否 | `string`, `size:18` | 身份证号 |
| `nation` | 否 | `string`, `max:20` | 民族 |
| `birth_date` | 否 | `date` | 出生日期 |
| `marital_status` | 否 | `integer`, `min:0`, `max:4` | 婚姻状况：`0=未知`、`1=未婚`、`2=已婚`、`3=离异`、`4=丧偶` |
| `political_status` | 否 | `string`, `max:20` | 政治面貌 |
| `native_place` | 否 | `string`, `max:100` | 籍贯 |
| `current_identity` | 否 | `integer`, `min:0` | 当前身份：`0=其他`、`1=职场人`、`2=学生`、`3=待业` |
| `work_start_date` | 否 | `date` | 参加工作日期 |
| `work_years` | 否 | `integer`, `min:0`, `max:80` | 工作年限 |
| `current_salary` | 否 | `string`, `max:50` | 当前/期望薪资 |
| `salary_remark` | 否 | `string`, `max:200` | 薪资备注 |
| `recruit_source` | 否 | `string`, `max:100` | 招聘信息来源 |
| `highest_education_level` | 否 | `integer`, `min:1`, `max:6` | 最高学历：`1=高中/中专`、`2=专科`、`3=本科`、`4=硕士`、`5=博士`、`6=其他` |
| `is_fresh_graduate` | 否 | `boolean` | 是否应届 |
| `expected_salary_min` | 否 | `numeric`, `min:0` | 期望薪资下限 |
| `expected_salary_max` | 否 | `numeric`, `min:0` | 期望薪资上限 |
| `expected_salary_unit` | 否 | `in:1,2,3` | 薪资单位：`1=月`、`2=日`、`3=时` |
| `household_register` | 否 | `string`, `max:100` | 户口所在地 |
| `household_register_detail` | 否 | `string`, `max:200` | 户口详细地址 |
| `current_residence_city` | 否 | `string`, `max:100` | 现居住城市 |
| `current_city_code` | 否 | `string`, `max:32` | 现居住城市编码 |
| `current_residence_detail` | 否 | `string`, `max:200` | 现居住地详细地址 |
| `residence_country` | 否 | `string`, `max:50` | 现居住国家/地区 |
| `phone` | 是 | `string`, `max:20` | 联系电话 |
| `email` | 是 | `email:rfc`, `max:100` | 邮箱 |
| `source_type` | 否 | `in:1,2,3,4` | 来源类型：`1=上传`、`2=解析`、`3=手工创建`、`4=导入` |
| `file_url` | 否 | `string`, `max:255` | 简历文件地址 |
| `file_name` | 否 | `string`, `max:255` | 简历文件名称 |
| `file_ext` | 否 | `string`, `max:16` | 文件后缀 |
| `text_content` | 否 | `string` | 简历文本内容 |
| `parsed_data` | 否 | `array` | 解析结构化结果 |
| `is_primary` | 否 | `boolean` | 是否主简历 |
| `status` | 否 | `in:0,1` | 状态：`0=停用`、`1=正常` |
| `extra` | 否 | `array` | 扩展字段 |

- 说明：`ResumeStoreRequest` 当前未对 `title` 做校验，但业务侧建议传 `title`（数据库字段为必填）。

### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "id": 1,
    "user_id": 1,
    "resume_no": "RC20260602120000ABC123",
    "title": "求职简历",
    "full_name": "张三",
    "source_type": 3,
    "is_primary": 1,
    "status": 1,
    "created_at": "2026-06-02T12:00:00.000000Z",
    "updated_at": "2026-06-02T12:00:00.000000Z"
  },
  "meta": {
    "timestamp": 1748865600.1234,
    "response_time": 0.0123
  }
}
```

### 规则

- 新增时自动生成 `resume_no`
- 若当前用户没有主简历，新建简历自动设为主简历（`is_primary = 1`）
- 若传入 `is_primary = 1`，会取消同用户其他主简历
- `source_type` 未传时默认 `3`
- `status` 未传时默认 `1`

---

## 4) 编辑简历

- 接口：`PUT /rc/resumes/{id}`
- 描述：编辑当前用户自己的简历；支持切换主简历

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | int | 简历 ID |

### 请求示例

```json
{
  "title": "更新后的简历名称",
  "full_name": "张三",
  "phone": "13800138000",
  "email": "zhangsan@example.com",
  "is_primary": 1
}
```

### 字段规则

- 以下规则与 `app/Rc/Requests/ResumeUpdateRequest.php` 对齐：

| 字段 | 是否必填 | 类型/规则 | 说明 |
|------|----------|-----------|------|
| `title` | 条件必填 | `sometimes`, `required`, `string`, `max:255` | 简历名称 |
| `full_name` | 条件必填 | `sometimes`, `required`, `string`, `max:50` | 姓名 |
| `avatar` | 否 | `sometimes`, `nullable`, `string`, `max:255` | 头像 OSS 路径 |
| `gender` | 否 | `sometimes`, `nullable`, `integer`, `min:0`, `max:2` | 性别枚举：`0=未知`、`1=男`、`2=女` |
| `id_card` | 否 | `sometimes`, `nullable`, `string`, `size:18` | 身份证号 |
| `nation` | 否 | `sometimes`, `nullable`, `string`, `max:20` | 民族 |
| `birth_date` | 否 | `sometimes`, `nullable`, `date` | 出生日期 |
| `birth_month` | 否 | `sometimes`, `nullable`, `date_format:Y-m` | 出生年月 |
| `age` | 否 | `sometimes`, `nullable`, `integer`, `min:0`, `max:120` | 年龄 |
| `marital_status` | 否 | `sometimes`, `nullable`, `integer`, `min:0`, `max:4` | 婚姻状况：`0=未知`、`1=未婚`、`2=已婚`、`3=离异`、`4=丧偶` |
| `political_status` | 否 | `sometimes`, `nullable`, `string`, `max:20` | 政治面貌 |
| `native_place` | 否 | `sometimes`, `nullable`, `string`, `max:100` | 籍贯 |
| `current_identity` | 否 | `sometimes`, `nullable`, `integer`, `min:0` | 当前身份：`0=其他`、`1=职场人`、`2=学生`、`3=待业` |
| `work_start_date` | 否 | `sometimes`, `nullable`, `date` | 参加工作日期 |
| `work_years` | 否 | `sometimes`, `nullable`, `integer`, `min:0`, `max:80` | 工作年限 |
| `current_salary` | 否 | `sometimes`, `nullable`, `string`, `max:50` | 当前/期望薪资 |
| `salary_remark` | 否 | `sometimes`, `nullable`, `string`, `max:200` | 薪资备注 |
| `recruit_source` | 否 | `sometimes`, `nullable`, `string`, `max:100` | 招聘信息来源 |
| `highest_education_level` | 否 | `sometimes`, `nullable`, `integer`, `min:1`, `max:6` | 最高学历：`1=高中/中专`、`2=专科`、`3=本科`、`4=硕士`、`5=博士`、`6=其他` |
| `is_fresh_graduate` | 否 | `sometimes`, `nullable`, `boolean` | 是否应届 |
| `expected_salary_min` | 否 | `sometimes`, `nullable`, `numeric`, `min:0` | 期望薪资下限 |
| `expected_salary_max` | 否 | `sometimes`, `nullable`, `numeric`, `min:0` | 期望薪资上限 |
| `expected_salary_unit` | 否 | `sometimes`, `nullable`, `in:1,2,3` | 薪资单位：`1=月`、`2=日`、`3=时` |
| `household_register` | 否 | `sometimes`, `nullable`, `string`, `max:100` | 户口所在地 |
| `household_register_detail` | 否 | `sometimes`, `nullable`, `string`, `max:200` | 户口详细地址 |
| `current_residence_city` | 否 | `sometimes`, `nullable`, `string`, `max:100` | 现居住城市 |
| `current_city_code` | 否 | `sometimes`, `nullable`, `string`, `max:32` | 现居住城市编码 |
| `current_residence_detail` | 否 | `sometimes`, `nullable`, `string`, `max:200` | 现居住地详细地址 |
| `residence_country` | 否 | `sometimes`, `nullable`, `string`, `max:50` | 现居住国家/地区 |
| `phone` | 条件必填 | `sometimes`, `required`, `string`, `max:20` | 联系电话 |
| `email` | 条件必填 | `sometimes`, `required`, `email:rfc`, `max:100` | 邮箱 |
| `source_type` | 否 | `sometimes`, `nullable`, `in:1,2,3,4` | 来源类型：`1=上传`、`2=解析`、`3=手工创建`、`4=导入` |
| `file_url` | 否 | `sometimes`, `nullable`, `string`, `max:255` | 简历文件地址 |
| `file_name` | 否 | `sometimes`, `nullable`, `string`, `max:255` | 简历文件名称 |
| `file_ext` | 否 | `sometimes`, `nullable`, `string`, `max:16` | 文件后缀 |
| `text_content` | 否 | `sometimes`, `nullable`, `string` | 简历文本内容 |
| `parsed_data` | 否 | `sometimes`, `nullable`, `array` | 解析结构化结果 |
| `is_primary` | 否 | `sometimes`, `nullable`, `boolean` | 是否主简历 |
| `status` | 否 | `sometimes`, `nullable`, `in:0,1` | 状态：`0=停用`、`1=正常` |
| `extra` | 否 | `sometimes`, `nullable`, `array` | 扩展字段 |

- 说明：`条件必填` 表示仅当该字段出现在请求体中时，字段值不能为空。
- 若传入 `is_primary = 1`，会将当前简历设为主简历，并取消同用户其他主简历。

### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "id": 1,
    "user_id": 1,
    "resume_no": "RC20260602120000ABC123",
    "title": "更新后的简历名称",
    "source_type": 3,
    "is_primary": 1,
    "status": 1,
    "created_at": "2026-06-02T12:00:00.000000Z",
    "updated_at": "2026-06-02T12:10:00.000000Z"
  },
  "meta": {
    "timestamp": 1748866200.1234,
    "response_time": 0.0123
  }
}
```

### 业务失败响应示例

```json
{
  "code": 404,
  "message": "简历不存在。",
  "meta": {
    "timestamp": 1748866200.1234,
    "response_time": 0.0123
  }
}
```

---

## 5) 求职意向列表

- 接口：`GET /rc/resumes/{id}/intentions`
- 描述：返回指定简历下所有求职意向，按 `updated_at DESC -> id DESC` 排序

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | int | 简历 ID |

### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "intentions": [
      {
        "id": 2,
        "resume_id": 1,
        "user_id": 1,
        "job_status": 3,
        "employment_type": 1,
        "expected_city_code": "310100",
        "expected_industry_codes": ["A01"],
        "expected_position_id": null,
        "salary_min": null,
        "salary_max": null,
        "salary_unit": 1,
        "available_date": null,
        "extra": null,
        "created_at": "2026-06-02T12:10:00.000000Z",
        "updated_at": "2026-06-02T12:10:00.000000Z"
      }
    ]
  },
  "meta": {
    "timestamp": 1748865600.1234,
    "response_time": 0.0123
  }
}
```

### 规则

- 仅允许访问当前用户自己的简历；否则返回 `404`
- 一份简历可拥有**多条**求职意向

---

## 6) 求职意向详情

- 接口：`GET /rc/resumes/{id}/intentions/{intentionId}`
- 描述：获取指定求职意向

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | int | 简历 ID |
| `intentionId` | int | 求职意向 ID |

### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "id": 1,
    "resume_id": 1,
    "user_id": 1,
    "job_status": 3,
    "employment_type": 1,
    "expected_city_code": "330100",
    "expected_industry_codes": ["A01", "A02"],
    "expected_position_id": 1001,
    "salary_min": "10000.00",
    "salary_max": "20000.00",
    "salary_unit": 1,
    "available_date": "2026-06-15T00:00:00.000000Z",
    "extra": null,
    "created_at": "2026-06-02T12:00:00.000000Z",
    "updated_at": "2026-06-02T12:00:00.000000Z"
  },
  "meta": {
    "timestamp": 1748865600.1234,
    "response_time": 0.0123
  }
}
```

### 业务失败响应示例

```json
{
  "code": 404,
  "message": "简历不存在。",
  "meta": { "timestamp": 1748865600.1234, "response_time": 0.0123 }
}
```

```json
{
  "code": 404,
  "message": "求职意向不存在。",
  "meta": { "timestamp": 1748865600.1234, "response_time": 0.0123 }
}
```

---

## 7) 新增求职意向

- 接口：`POST /rc/resumes/{id}/intentions`
- 描述：为指定简历新增一条求职意向

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | int | 简历 ID |

### 字段规则

以下规则与 `app/Rc/Requests/ResumeIntentionUpsertRequest.php` 对齐：

| 字段 | 是否必填 | 类型/规则 | 说明 |
|------|----------|-----------|------|
| `job_status` | 否 | `sometimes`, `nullable`, `in:1,2,3,4` | 求职状态：`1=在职考虑机会`、`2=在职不考虑`、`3=离职找工作`、`4=应届生` |
| `employment_type` | 否 | `sometimes`, `nullable`, `in:1,2,3` | 工作类型：`1=全职`、`2=兼职`、`3=实习` |
| `expected_city_code` | 否 | `sometimes`, `nullable`, `string`, `max:32` | 期望城市编码 |
| `expected_industry_codes` | 否 | `sometimes`, `nullable`, `array`, `max:10` | 期望行业代码列表（多选，每项 `max:64`） |
| `expected_position_id` | 否 | `sometimes`, `nullable`, `integer`, `min:1` | 期望职位 ID |
| `salary_min` | 否 | `sometimes`, `nullable`, `numeric`, `min:0` | 最低期望薪资 |
| `salary_max` | 否 | `sometimes`, `nullable`, `numeric`, `min:0` | 最高期望薪资 |
| `salary_unit` | 否 | `sometimes`, `nullable`, `in:1,2,3` | 薪资单位：`1=月`、`2=日`、`3=时` |
| `available_date` | 否 | `sometimes`, `nullable`, `date` | 可到岗日期 |
| `extra` | 否 | `sometimes`, `nullable`, `array` | 扩展字段 |

### 请求示例

```json
{
  "job_status": 3,
  "employment_type": 1,
  "expected_city_code": "330100",
  "expected_industry_codes": ["A01", "A02"],
  "expected_position_id": 1001,
  "salary_min": 10000,
  "salary_max": 20000,
  "salary_unit": 1,
  "available_date": "2026-06-15"
}
```

### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "id": 1,
    "resume_id": 1,
    "user_id": 1,
    "job_status": 3,
    "employment_type": 1,
    "expected_city_code": "330100",
    "expected_industry_codes": ["A01", "A02"],
    "expected_position_id": 1001,
    "salary_min": "10000.00",
    "salary_max": "20000.00",
    "salary_unit": 1,
    "available_date": "2026-06-15T00:00:00.000000Z",
    "extra": null,
    "created_at": "2026-06-02T12:00:00.000000Z",
    "updated_at": "2026-06-02T12:00:00.000000Z"
  },
  "meta": {
    "timestamp": 1748865600.1234,
    "response_time": 0.0123
  }
}
```

### 规则

- 同一份简历可有多条求职意向，无数量限制
- `job_status` 未传时默认 `1`（在职考虑机会）
- `salary_unit` 未传时默认 `1`（月）

---

## 8) 编辑求职意向

- 接口：`PUT /rc/resumes/{id}/intentions/{intentionId}`
- 描述：修改指定求职意向，支持部分更新

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | int | 简历 ID |
| `intentionId` | int | 求职意向 ID |

### 字段规则

与新增接口字段规则完全一致，所有字段均为 `sometimes`（部分更新）。

### 请求示例

```json
{
  "job_status": 1,
  "salary_max": 25000
}
```

### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "id": 1,
    "resume_id": 1,
    "user_id": 1,
    "job_status": 1,
    "employment_type": 1,
    "expected_city_code": "330100",
    "expected_industry_codes": ["A01", "A02"],
    "expected_position_id": 1001,
    "salary_min": "10000.00",
    "salary_max": "25000.00",
    "salary_unit": 1,
    "available_date": "2026-06-15T00:00:00.000000Z",
    "extra": null,
    "created_at": "2026-06-02T12:00:00.000000Z",
    "updated_at": "2026-06-02T12:05:00.000000Z"
  },
  "meta": {
    "timestamp": 1748866200.1234,
    "response_time": 0.0123
  }
}
```

### 业务失败响应示例

```json
{
  "code": 404,
  "message": "简历不存在。",
  "meta": { "timestamp": 1748866200.1234, "response_time": 0.0123 }
}
```

```json
{
  "code": 404,
  "message": "求职意向不存在。",
  "meta": { "timestamp": 1748866200.1234, "response_time": 0.0123 }
}
```

---

## 9) 删除求职意向

- 接口：`DELETE /rc/resumes/{id}/intentions/{intentionId}`
- 描述：软删除指定求职意向

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | int | 简历 ID |
| `intentionId` | int | 求职意向 ID |

### 成功响应示例

```json
{
  "code": 200,
  "data": {},
  "meta": {
    "timestamp": 1748866200.1234,
    "response_time": 0.0123
  }
}
```

### 业务失败响应示例

```json
{
  "code": 404,
  "message": "求职意向不存在。",
  "meta": { "timestamp": 1748866200.1234, "response_time": 0.0123 }
}
```

---

## 10) 工作经历列表

- 接口：`GET /rc/resumes/{id}/works`
- 描述：返回指定简历下所有工作经历，按 `sort DESC -> id DESC` 排序

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | int | 简历 ID |

### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "works": [
      {
        "id": 2,
        "resume_id": 1,
        "user_id": 1,
        "company_name": "杭州示例科技有限公司",
        "department": "研发中心",
        "position": "高级后端工程师",
        "employment_type": 1,
        "start_date": "2022-01-01T00:00:00.000000Z",
        "end_date": "2024-05-31T00:00:00.000000Z",
        "is_current": 0,
        "description": "负责招聘系统后端研发。",
        "sort": 10,
        "extra": null,
        "created_at": "2026-06-02T12:10:00.000000Z",
        "updated_at": "2026-06-02T12:10:00.000000Z"
      }
    ]
  },
  "meta": {
    "timestamp": 1748865600.1234,
    "response_time": 0.0123
  }
}
```

### 规则

- 仅允许访问当前用户自己的简历；否则返回 `404`
- 一份简历可拥有**多条**工作经历

---

## 11) 工作经历详情

- 接口：`GET /rc/resumes/{id}/works/{workId}`
- 描述：获取指定工作经历

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | int | 简历 ID |
| `workId` | int | 工作经历 ID |

### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "id": 1,
    "resume_id": 1,
    "user_id": 1,
    "company_name": "杭州示例科技有限公司",
    "department": "研发中心",
    "position": "后端工程师",
    "employment_type": 1,
    "start_date": "2020-07-01T00:00:00.000000Z",
    "end_date": "2022-12-31T00:00:00.000000Z",
    "is_current": 0,
    "description": "负责招聘系统 API 开发与维护。",
    "sort": 0,
    "extra": null,
    "created_at": "2026-06-02T12:00:00.000000Z",
    "updated_at": "2026-06-02T12:00:00.000000Z"
  },
  "meta": {
    "timestamp": 1748865600.1234,
    "response_time": 0.0123
  }
}
```

### 业务失败响应示例

```json
{
  "code": 404,
  "message": "简历不存在。",
  "meta": { "timestamp": 1748865600.1234, "response_time": 0.0123 }
}
```

```json
{
  "code": 404,
  "message": "工作经验不存在。",
  "meta": { "timestamp": 1748865600.1234, "response_time": 0.0123 }
}
```

---

## 12) 新增工作经历

- 接口：`POST /rc/resumes/{id}/works`
- 描述：为指定简历新增一条工作经历

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | int | 简历 ID |

### 字段规则

以下规则与 `app/Rc/Requests/ResumeWorkStoreRequest.php` 对齐：

| 字段 | 是否必填 | 类型/规则 | 说明 |
|------|----------|-----------|------|
| `company_name` | 是 | `required`, `string`, `max:255` | 公司名称 |
| `department` | 否 | `nullable`, `string`, `max:255` | 部门名称 |
| `position` | 是 | `required`, `string`, `max:255` | 职位名称 |
| `employment_type` | 否 | `nullable`, `enum` | 工作类型：`1=全职`、`2=兼职`、`3=实习` |
| `start_date` | 是 | `required`, `date` | 开始日期 |
| `end_date` | 否 | `nullable`, `date`, `after_or_equal:start_date` | 结束日期，需不早于开始日期 |
| `is_current` | 否 | `nullable`, `boolean` | 是否至今 |
| `description` | 否 | `nullable`, `string` | 工作描述 |
| `sort` | 否 | `nullable`, `integer`, `min:0`, `max:9999` | 排序值 |
| `extra` | 否 | `nullable`, `array` | 扩展字段 |

### 请求示例

```json
{
  "company_name": "杭州示例科技有限公司",
  "department": "研发中心",
  "position": "后端工程师",
  "employment_type": 1,
  "start_date": "2022-01-01",
  "end_date": "2024-05-31",
  "is_current": 0,
  "description": "负责招聘系统 API 开发。",
  "sort": 10
}
```

### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "id": 1,
    "resume_id": 1,
    "user_id": 1,
    "company_name": "杭州示例科技有限公司",
    "department": "研发中心",
    "position": "后端工程师",
    "employment_type": 1,
    "start_date": "2022-01-01T00:00:00.000000Z",
    "end_date": "2024-05-31T00:00:00.000000Z",
    "is_current": 0,
    "description": "负责招聘系统 API 开发。",
    "sort": 10,
    "extra": null,
    "created_at": "2026-06-02T12:00:00.000000Z",
    "updated_at": "2026-06-02T12:00:00.000000Z"
  },
  "meta": {
    "timestamp": 1748865600.1234,
    "response_time": 0.0123
  }
}
```

### 规则

- 同一份简历可有多条工作经历，无数量限制
- `employment_type` 未传时默认 `1`（全职）
- `is_current` 未传时默认 `0`
- `sort` 未传时默认 `0`

---

## 13) 编辑工作经历

- 接口：`PUT /rc/resumes/{id}/works/{workId}`
- 描述：修改指定工作经历，支持部分更新

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | int | 简历 ID |
| `workId` | int | 工作经历 ID |

### 字段规则

以下规则与 `app/Rc/Requests/ResumeWorkUpdateRequest.php` 对齐：

| 字段 | 是否必填 | 类型/规则 | 说明 |
|------|----------|-----------|------|
| `company_name` | 条件必填 | `sometimes`, `required`, `string`, `max:255` | 公司名称 |
| `department` | 否 | `sometimes`, `nullable`, `string`, `max:255` | 部门名称 |
| `position` | 条件必填 | `sometimes`, `required`, `string`, `max:255` | 职位名称 |
| `employment_type` | 否 | `sometimes`, `nullable`, `enum` | 工作类型：`1=全职`、`2=兼职`、`3=实习` |
| `start_date` | 条件必填 | `sometimes`, `required`, `date` | 开始日期 |
| `end_date` | 否 | `sometimes`, `nullable`, `date`, `after_or_equal:start_date` | 结束日期，需不早于开始日期 |
| `is_current` | 否 | `sometimes`, `nullable`, `boolean` | 是否至今 |
| `description` | 否 | `sometimes`, `nullable`, `string` | 工作描述 |
| `sort` | 否 | `sometimes`, `nullable`, `integer`, `min:0`, `max:9999` | 排序值 |
| `extra` | 否 | `sometimes`, `nullable`, `array` | 扩展字段 |

- 说明：`条件必填` 表示仅当该字段出现在请求体中时，字段值不能为空。

### 请求示例

```json
{
  "position": "高级后端工程师",
  "is_current": 1,
  "end_date": null
}
```

### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "id": 1,
    "resume_id": 1,
    "user_id": 1,
    "company_name": "杭州示例科技有限公司",
    "department": "研发中心",
    "position": "高级后端工程师",
    "employment_type": 1,
    "start_date": "2022-01-01T00:00:00.000000Z",
    "end_date": null,
    "is_current": 1,
    "description": "负责招聘系统 API 开发。",
    "sort": 10,
    "extra": null,
    "created_at": "2026-06-02T12:00:00.000000Z",
    "updated_at": "2026-06-02T12:05:00.000000Z"
  },
  "meta": {
    "timestamp": 1748866200.1234,
    "response_time": 0.0123
  }
}
```

### 业务失败响应示例

```json
{
  "code": 404,
  "message": "简历不存在。",
  "meta": { "timestamp": 1748866200.1234, "response_time": 0.0123 }
}
```

```json
{
  "code": 404,
  "message": "工作经验不存在。",
  "meta": { "timestamp": 1748866200.1234, "response_time": 0.0123 }
}
```

---

## 14) 删除工作经历

- 接口：`DELETE /rc/resumes/{id}/works/{workId}`
- 描述：软删除指定工作经历

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | int | 简历 ID |
| `workId` | int | 工作经历 ID |

### 成功响应示例

```json
{
  "code": 200,
  "data": {},
  "meta": {
    "timestamp": 1748866200.1234,
    "response_time": 0.0123
  }
}
```

### 业务失败响应示例

```json
{
  "code": 404,
  "message": "简历不存在。",
  "meta": { "timestamp": 1748866200.1234, "response_time": 0.0123 }
}
```

```json
{
  "code": 404,
  "message": "工作经验不存在。",
  "meta": { "timestamp": 1748866200.1234, "response_time": 0.0123 }
}
```

---

## 15) 教育经历列表

- 接口：`GET /rc/resumes/{id}/educations`
- 描述：返回指定简历下所有教育经历，按 `sort DESC -> id DESC` 排序

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | int | 简历 ID |

### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "educations": [
      {
        "id": 2,
        "resume_id": 1,
        "user_id": 1,
        "school_name": "浙江大学",
        "major": "计算机科学与技术",
        "degree": 3,
        "education_type": 1,
        "start_date": "2018-09-01T00:00:00.000000Z",
        "end_date": "2022-06-30T00:00:00.000000Z",
        "is_current": 0,
        "description": "主修软件工程方向。",
        "sort": 10,
        "extra": null,
        "created_at": "2026-06-02T12:10:00.000000Z",
        "updated_at": "2026-06-02T12:10:00.000000Z"
      }
    ]
  },
  "meta": {
    "timestamp": 1748865600.1234,
    "response_time": 0.0123
  }
}
```

### 规则

- 仅允许访问当前用户自己的简历；否则返回 `404`
- 一份简历可拥有**多条**教育经历

---

## 16) 教育经历详情

- 接口：`GET /rc/resumes/{id}/educations/{educationId}`
- 描述：获取指定教育经历

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | int | 简历 ID |
| `educationId` | int | 教育经历 ID |

### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "id": 1,
    "resume_id": 1,
    "user_id": 1,
    "school_name": "浙江大学",
    "major": "计算机科学与技术",
    "degree": 3,
    "education_type": 1,
    "start_date": "2018-09-01T00:00:00.000000Z",
    "end_date": "2022-06-30T00:00:00.000000Z",
    "is_current": 0,
    "description": "主修软件工程方向。",
    "sort": 0,
    "extra": null,
    "created_at": "2026-06-02T12:00:00.000000Z",
    "updated_at": "2026-06-02T12:00:00.000000Z"
  },
  "meta": {
    "timestamp": 1748865600.1234,
    "response_time": 0.0123
  }
}
```

### 业务失败响应示例

```json
{
  "code": 404,
  "message": "简历不存在。",
  "meta": { "timestamp": 1748865600.1234, "response_time": 0.0123 }
}
```

```json
{
  "code": 404,
  "message": "教育经历不存在。",
  "meta": { "timestamp": 1748865600.1234, "response_time": 0.0123 }
}
```

---

## 17) 新增教育经历

- 接口：`POST /rc/resumes/{id}/educations`
- 描述：为指定简历新增一条教育经历

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | int | 简历 ID |

### 字段规则

以下规则与 `app/Rc/Requests/ResumeEducationStoreRequest.php` 对齐：

| 字段 | 是否必填 | 类型/规则 | 说明 |
|------|----------|-----------|------|
| `school_name` | 是 | `required`, `string`, `max:255` | 学校名称 |
| `major` | 否 | `nullable`, `string`, `max:255` | 专业名称 |
| `degree` | 否 | `nullable`, `enum` | 学历：复用 `highest_education_level` 枚举 |
| `education_type` | 否 | `nullable`, `in:1,2` | 学习方式：`1=全日制`、`2=非全日制` |
| `start_date` | 是 | `required`, `date` | 开始日期 |
| `end_date` | 否 | `nullable`, `date`, `after_or_equal:start_date` | 结束日期，需不早于开始日期 |
| `is_current` | 否 | `nullable`, `boolean` | 是否至今 |
| `description` | 否 | `nullable`, `string` | 经历描述 |
| `sort` | 否 | `nullable`, `integer`, `min:0`, `max:9999` | 排序值 |
| `extra` | 否 | `nullable`, `array` | 扩展字段 |

### 请求示例

```json
{
  "school_name": "浙江大学",
  "major": "计算机科学与技术",
  "degree": 3,
  "education_type": 1,
  "start_date": "2018-09-01",
  "end_date": "2022-06-30",
  "is_current": 0,
  "description": "主修软件工程方向。",
  "sort": 10
}
```

### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "id": 1,
    "resume_id": 1,
    "user_id": 1,
    "school_name": "浙江大学",
    "major": "计算机科学与技术",
    "degree": 3,
    "education_type": 1,
    "start_date": "2018-09-01T00:00:00.000000Z",
    "end_date": "2022-06-30T00:00:00.000000Z",
    "is_current": 0,
    "description": "主修软件工程方向。",
    "sort": 10,
    "extra": null,
    "created_at": "2026-06-02T12:00:00.000000Z",
    "updated_at": "2026-06-02T12:00:00.000000Z"
  },
  "meta": {
    "timestamp": 1748865600.1234,
    "response_time": 0.0123
  }
}
```

### 规则

- 同一份简历可有多条教育经历，无数量限制
- `education_type` 未传时默认 `1`（全日制）
- `is_current` 未传时默认 `0`
- `sort` 未传时默认 `0`

---

## 18) 编辑教育经历

- 接口：`PUT /rc/resumes/{id}/educations/{educationId}`
- 描述：修改指定教育经历，支持部分更新

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | int | 简历 ID |
| `educationId` | int | 教育经历 ID |

### 字段规则

以下规则与 `app/Rc/Requests/ResumeEducationUpdateRequest.php` 对齐：

| 字段 | 是否必填 | 类型/规则 | 说明 |
|------|----------|-----------|------|
| `school_name` | 条件必填 | `sometimes`, `required`, `string`, `max:255` | 学校名称 |
| `major` | 否 | `sometimes`, `nullable`, `string`, `max:255` | 专业名称 |
| `degree` | 否 | `sometimes`, `nullable`, `enum` | 学历：复用 `highest_education_level` 枚举 |
| `education_type` | 否 | `sometimes`, `nullable`, `in:1,2` | 学习方式：`1=全日制`、`2=非全日制` |
| `start_date` | 条件必填 | `sometimes`, `required`, `date` | 开始日期 |
| `end_date` | 否 | `sometimes`, `nullable`, `date`, `after_or_equal:start_date` | 结束日期，需不早于开始日期 |
| `is_current` | 否 | `sometimes`, `nullable`, `boolean` | 是否至今 |
| `description` | 否 | `sometimes`, `nullable`, `string` | 经历描述 |
| `sort` | 否 | `sometimes`, `nullable`, `integer`, `min:0`, `max:9999` | 排序值 |
| `extra` | 否 | `sometimes`, `nullable`, `array` | 扩展字段 |

- 说明：`条件必填` 表示仅当该字段出现在请求体中时，字段值不能为空。

### 请求示例

```json
{
  "major": "软件工程",
  "is_current": 1,
  "end_date": null
}
```

### 成功响应示例

```json
{
  "code": 200,
  "data": {
    "id": 1,
    "resume_id": 1,
    "user_id": 1,
    "school_name": "浙江大学",
    "major": "软件工程",
    "degree": 3,
    "education_type": 1,
    "start_date": "2018-09-01T00:00:00.000000Z",
    "end_date": null,
    "is_current": 1,
    "description": "主修软件工程方向。",
    "sort": 10,
    "extra": null,
    "created_at": "2026-06-02T12:00:00.000000Z",
    "updated_at": "2026-06-02T12:05:00.000000Z"
  },
  "meta": {
    "timestamp": 1748866200.1234,
    "response_time": 0.0123
  }
}
```

### 业务失败响应示例

```json
{
  "code": 404,
  "message": "简历不存在。",
  "meta": { "timestamp": 1748866200.1234, "response_time": 0.0123 }
}
```

```json
{
  "code": 404,
  "message": "教育经历不存在。",
  "meta": { "timestamp": 1748866200.1234, "response_time": 0.0123 }
}
```

---

## 19) 删除教育经历

- 接口：`DELETE /rc/resumes/{id}/educations/{educationId}`
- 描述：软删除指定教育经历

### 路径参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | int | 简历 ID |
| `educationId` | int | 教育经历 ID |

### 成功响应示例

```json
{
  "code": 200,
  "data": {},
  "meta": {
    "timestamp": 1748866200.1234,
    "response_time": 0.0123
  }
}
```

### 业务失败响应示例

```json
{
  "code": 404,
  "message": "简历不存在。",
  "meta": { "timestamp": 1748866200.1234, "response_time": 0.0123 }
}
```

```json
{
  "code": 404,
  "message": "教育经历不存在。",
  "meta": { "timestamp": 1748866200.1234, "response_time": 0.0123 }
}
```

---

## 前端对接建议

- 列表页读取 `data.data`
- 分页读取 `data.current_page`、`data.per_page`、`data.total`、`data.last_page`
- 详情、新增、编辑接口的简历对象均直接位于 `data`
- `is_primary` 建议做成单选/开关，避免误操作
- 求职意向列表读取 `data.intentions`；详情/新增/编辑直接位于 `data`
- 工作经历列表读取 `data.works`；详情/新增/编辑直接位于 `data`
- 教育经历列表读取 `data.educations`；详情/新增/编辑直接位于 `data`
- 一份简历支持多条求职意向，前端可做"添加/删除意向"功能
- 一份简历支持多条工作经历、教育经历，前端可做"添加/删除经历"功能

## 相关字典接口

- `GET /cms/home/rc/positions`：返回 `data.positions` 树状结构
- `GET /cms/home/rc/industries`：返回 `data.industries` 树状结构
- `GET /rc/meta`：聚合返回 `cities`、`industries`、`positions`


