# 企业（招聘方绑定/注册）

## 说明

- 路由前缀：`/rc`
- 鉴权：均需 `Bearer Token`，并使用 `auth:rc`
- 适用对象：招聘方身份（`identity_type = 2`）
- 成功响应：`api_response()`，结构为 `code` + `data` + `meta`
- 业务错误：`error()`，结构为 `code` + `message` + `meta`

> 绑定或注册成功后，招聘方身份的 `has_basic_info` 为 `true`（`organization_type = company` 且 `organization_id` 不为空）。

### 前端入参说明

注册接口仅提交企业主体字段与营业执照 OSS 路径；**证件、联系人由服务端自动写入** `company_licenses`、`company_contacts`（未知字段使用默认值，见 [服务端自动填充](#服务端自动填充)）。

绑定接口仅提交 `company_id` 与 `job_title`，不重复写入证件/联系人（企业资料由 lookup 或历史注册产生）。

注册或绑定成功后，服务端会自动创建 `company_profiles` 草稿记录（`profile_status = 0`），招聘方可通过 `PUT /rc/companies/profile` 完善展示资料。

营业执照需先 `POST /rc/upload`，将 `data.path` 填入 `licenses_file_path`；Logo 同理填入 `profile.logo`。

---

## 服务端自动填充

注册企业（`POST /rc/companies`）时自动创建：

### `company_licenses`（1 条营业执照）

| 字段 | 默认值来源 |
|------|------------|
| `license_type` | `1`（营业执照） |
| `name` | `营业执照` |
| `license_no` | `credit_code` |
| `file_url` / `file_name` / `file_ext` | 解析 `licenses_file_path`（未传则为 `null`） |
| `is_primary` | `1` |
| `issuer` / `issue_date` / `expire_date` | `null` |

### `company_contacts`（2 条）

| 记录 | `contact_type` | 默认填充 |
|------|----------------|----------|
| 法定代表人 | `1` | `name` ← `legal_person`，`phone` ← `contact_phone`，`position` = 法定代表人，`address` ← `address`，`is_primary` = 1 |
| 企业联系人 | `3` | 同上，`position` = 企业联系人，`is_primary` = 0 |

### `company_profiles`（1 条草稿）

| 字段 | 默认值 |
|------|--------|
| `profile_status` | `0`（草稿） |
| 其他展示字段 | `null` / 空数组 |

---

## 公共数据结构

### 企业对象（`RcCompanyResource`）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | 企业 ID |
| `name` | string | 企业名称 |
| `credit_code` | string\|null | 统一社会信用代码 |
| `legal_person` | string\|null | 法人姓名 |
| `contact_phone` | string\|null | 联系电话 |
| `address` | string\|null | 企业地址 |
| `status` | int | 见 [CompanyStatus](#companystatus) |
| `licenses` | array | 证件列表（lookup / 注册 / 绑定成功时返回） |
| `contacts` | array | 联系人列表 |
| `profile` | object\|null | 招聘展示资料（预加载时返回，见 [企业资料对象](#企业资料对象)） |
| `created_at` / `updated_at` | string\|null | 时间 |

### 企业资料对象（`RcCompanyProfileResource`）

| 字段 | 类型 | 说明 |
|------|------|------|
| `short_name` | string\|null | 企业简称/品牌名 |
| `logo` | string\|null | Logo OSS 路径 |
| `display_logo` | string\|null | Logo 可访问 URL |
| `city_code` | string\|null | 主办公城市编码 |
| `scale_type` | int\|null | 公司规模，见 [CompanyScaleType](#companyscaletype) |
| `scale_type_label` | string\|null | 规模中文 |
| `nature_type` | int\|null | 公司性质，见 [CompanyNatureType](#companynaturetype) |
| `nature_type_label` | string\|null | 性质中文 |
| `industry_codes` | array | 所属行业 codes |
| `founded_at` | string\|null | 成立日期 `Y-m-d` |
| `website` | string\|null | 官网 |
| `introduction` | string\|null | 企业简介 |
| `benefit_tags` | array | 福利标签 codes |
| `benefit_tag_labels` | array | 福利标签中文 |
| `funding_stage` | int\|null | 融资阶段，见 [CompanyFundingStage](#companyfundingstage) |
| `funding_stage_label` | string\|null | 融资阶段中文 |
| `profile_status` | int | 资料状态，见 [CompanyProfileStatus](#companyprofilestatus) |
| `profile_status_label` | string\|null | 资料状态中文 |
| `extra` | object\|null | 扩展字段 |
| `created_at` / `updated_at` | string\|null | 时间 |

> 字典数据来源：`GET /rc/meta/companies` 或 `GET /rc/meta` 中的 `company_scales`、`company_natures`、`company_funding_stages`、`company_benefit_tags`。

### 企业证件 / 联系人对象

字段说明见历史版本；`file_url` 为 OSS 相对路径，`display_file_url` 为可访问 URL。

### 身份对象（`identity`）

| 字段 | 类型 | 说明 |
|------|------|------|
| `organization_type` | string\|null | 企业为 `company` |
| `organization_id` | int\|null | 企业 ID |
| `job_title` | string\|null | 岗位名称 |
| `has_basic_info` | bool | 是否已完成基础信息 |

---

## 1) 查询企业

- 接口：`GET /rc/companies/lookup`

### 请求参数（Query）

| 字段 | 是否必填 | 规则 | 说明 |
|------|----------|------|------|
| `credit_code` | 二选一 | `string`, `size:18` | 统一社会信用代码（精确查询） |
| `name` | 二选一 | `string`, `min:2`, `max:255` | 企业名称（模糊搜索） |

> `credit_code` 与 `name` 至少传一个；传 `credit_code` 时走精确查询，传 `name` 时返回匹配列表。

### 成功响应 `data`

**按 `credit_code` 查询**

| 字段 | 说明 |
|------|------|
| `exists` | 是否已注册 |
| `company` | 企业对象（含 `licenses`、`contacts`），不存在时为 `null` |

**按 `name` 搜索**

| 字段 | 说明 |
|------|------|
| `companies` | 匹配企业列表（基础字段） |

---

## 2) 绑定已存在企业

- 接口：`POST /rc/companies/bind`
- Content-Type：`application/json`

### 请求参数

| 字段 | 是否必填 | 规则 | 说明 |
|------|----------|------|------|
| `company_id` | 是 | `required`, `exists:companies,id`（未软删） | 企业 ID（来自 lookup 的 `company.id`） |
| `job_title` | 是 | `required`, `string`, `max:50` | 岗位名称 |

### 请求示例

```json
{
  "company_id": 12,
  "job_title": "招聘经理"
}
```

### 成功响应 `data`

| 字段 | 说明 |
|------|------|
| `company` | 企业对象（含已有证件、联系人） |
| `identity` | 绑定后的招聘方身份 |

### 业务失败

| `code` | `message` |
|--------|-----------|
| `422` | 请先切换为招聘方身份。 |
| `422` | 您已绑定该企业。 |
| `422` | 该企业已被禁用，无法绑定。 |

> 当前 Token 对应招聘方身份已绑定其他企业时，会**新建一条**招聘方身份行并绑定目标企业（同一用户可拥有多家企业身份，切换见 `POST /rc/auth/refresh-token` + `identity_id`）。

`company_id` 不存在时返回 HTTP `422` 参数校验错误（`errors.company_id`）。

---

## 3) 注册企业并绑定

- 接口：`POST /rc/companies`
- Content-Type：`application/json`

### 请求参数

| 字段 | 是否必填 | 规则 | 说明 |
|------|----------|------|------|
| `name` | 是 | `required`, `string`, `max:255` | 企业名称 |
| `credit_code` | 是 | `required`, `string`, `size:18` | 统一社会信用代码 |
| `legal_person` | 是 | `required`, `string`, `max:255` | 法人姓名 |
| `contact_phone` | 是 | `required`, `string`, `max:255` | 联系电话 |
| `address` | 否 | `nullable`, `string` | 企业地址 |
| `job_title` | 是 | `required`, `string`, `max:50` | 岗位名称 |
| `licenses_file_path` | 否 | `nullable`, `string`, `max:500` | 营业执照 OSS 路径（`POST /rc/upload` 的 `path`） |

### 请求示例

```json
{
  "name": "南昌示例科技有限公司",
  "credit_code": "91360100MA0000000X",
  "legal_person": "李四",
  "contact_phone": "13900000000",
  "address": "南昌市高新区示例路 88 号",
  "job_title": "HR 总监",
  "licenses_file_path": "uploads/rc/file/2026/06/04/license.pdf"
}
```

### 成功响应

- `data.company`：含自动生成的 `licenses`（营业执照）、`contacts`（法定代表人 + 企业联系人）
- `data.identity`：已绑定身份
- `company.status` = `2`（审批中）

### 业务失败

| `code` | `message` |
|--------|-----------|
| `422` | 请先切换为招聘方身份。 |
| `422` | 您已绑定该企业。 |
| `422` | 企业已存在，请直接绑定。 |

> 当前身份已绑定企业时，注册新企业同样会新建身份行；若信用代码对应企业已在库中且您尚未绑定，请走「绑定已存在企业」接口。

---

## 4) 获取当前企业招聘资料

- 接口：`GET /rc/companies/profile`
- 描述：返回当前绑定企业的 `company_profiles` 记录；不存在时自动创建草稿

### 业务失败

| `code` | `message` |
|--------|-----------|
| `422` | 请先切换为招聘方身份并绑定企业。 |

---

## 5) 更新当前企业招聘资料

- 接口：`PUT /rc/companies/profile`
- Content-Type：`application/json`

### 请求参数

| 字段 | 是否必填 | 规则 | 说明 |
|------|----------|------|------|
| `short_name` | 否 | `nullable`, `string`, `max:100` | 企业简称 |
| `logo` | 否 | `nullable`, `string`, `max:500` | Logo OSS 路径 |
| `city_code` | 否 | `nullable`, `string`, `max:32` | 主办公城市 |
| `scale_type` | 否 | `nullable`, `integer`, Enum | 公司规模 |
| `nature_type` | 否 | `nullable`, `integer`, Enum | 公司性质 |
| `industry_codes` | 否 | `nullable`, `array`, `max:10` | 行业 codes，需存在于 `rc_industries.code` |
| `founded_at` | 否 | `nullable`, `date` | 成立日期 |
| `website` | 否 | `nullable`, `url`, `max:255` | 官网 |
| `introduction` | 否 | `nullable`, `string`, `max:5000` | 企业简介 |
| `benefit_tags` | 否 | `nullable`, `array`, `max:20` | 福利标签 codes |
| `funding_stage` | 否 | `nullable`, `integer`, Enum | 融资阶段 |

### 规则

- 当 `scale_type`、`nature_type`、`introduction`、`logo` 均已填写时，`profile_status` 自动从 `0`（草稿）变为 `1`（已完善）

---

## 前端推荐流程

1. `POST /rc/auth/refresh-token`，`identity_type = 2`
2. `GET /rc/companies/lookup?credit_code=...`
3. **已存在**：记下 `company.id` → `POST /rc/companies/bind`（`company_id` + `job_title`）
4. **不存在**：上传执照 → `POST /rc/companies`（主体字段 + `licenses_file_path` + `job_title`）
5. `GET /rc/auth/me` 确认 `has_basic_info`
6. `PUT /rc/companies/profile` 完善企业展示资料

---

## 枚举参考

### CompanyStatus

| 值 | 含义 |
|----|------|
| `0` | 禁用 |
| `1` | 启用 |
| `2` | 审批中（Rc 注册默认） |

### CompanyLicenseType

| 值 | 含义 |
|----|------|
| `1` | 营业执照 |
| `2` | 食品经营许可证 |
| `3` | 资质证书 |
| `4` | 其他 |

### CompanyContactType

| 值 | 含义 |
|----|------|
| `1` | 法定代表人 |
| `2` | 股东 |
| `3` | 联系人 |
| `4` | 实际控制人 |
| `5` | 其他 |

### CompanyScaleType

| 值 | 含义 |
|----|------|
| `1` | 0-20人 |
| `2` | 20-99人 |
| `3` | 100-499人 |
| `4` | 500-999人 |
| `5` | 1000-9999人 |
| `6` | 10000人以上 |

### CompanyNatureType

| 值 | 含义 |
|----|------|
| `1` | 民营企业 |
| `2` | 国有企业 |
| `3` | 外资企业 |
| `4` | 合资企业 |
| `5` | 事业单位 |
| `6` | 非营利组织 |
| `7` | 其他 |

### CompanyFundingStage

| 值 | 含义 |
|----|------|
| `1` | 未融资 |
| `2` | 天使轮 |
| `3` | A轮 |
| `4` | B轮 |
| `5` | C轮 |
| `6` | D轮及以上 |
| `7` | 已上市 |
| `8` | 不需要融资 |

### CompanyProfileStatus

| 值 | 含义 |
|----|------|
| `0` | 草稿 |
| `1` | 已完善 |
| `2` | 审核中 |

### CompanyBenefitTag（`value` 为 string code）

| value | 含义 |
|-------|------|
| `social_insurance` | 五险一金 |
| `housing_fund` | 住房公积金 |
| `weekend_off` | 双休 |
| `flexible_work` | 弹性工作 |
| `annual_bonus` | 年终奖 |
| `paid_leave` | 带薪年假 |
| `meal_allowance` | 餐补 |
| `transport_allowance` | 交通补贴 |
| `stock_option` | 股票期权 |
| `training` | 培训机会 |
